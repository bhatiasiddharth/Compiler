fn add(x:i32, y:i32) -> i32 {
   let mut ans = 0;
   ans = x + y;
   return ans;
}

fn add(x:string, y:string) -> string {
   let mut ans = 5;
   //let boolean x=true;
   ans = x + y;
   return ans;
}

main() {
	// Comments
	// Comments2
	let mut str1 = "radsar";
	if ((str1 == str1[5]) OR (str1 <= str.reverse(5,"abc",6,7,9))) {
	x=5+6;
		print("Yes");
	}
	else {print("bye");}
	while(x < 5) {
   print(x);
   x = x+1; 
}

(x,y,z) = (1,2,3);

}