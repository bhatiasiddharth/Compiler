main() {
	let mut (first,second,i,temp)=(0,1,1,0);
	let mut n=0;
	print("Enter N");
	scan(n);
	print(" ");
	if(n==1)
	{
		print(0);
	}
	elseif(n==2)
	{
		print(0);
		print(1);
	}
	else
	{
		n=n-2;
		print(0);
		print(1);	
		while(i<=n)
		{
			temp=first;
			first=second;
			second=temp+second;
			print(second);
			i=i+1;
		}	
	}
}